# 技術検証: セッションの共有・合流は成立するか

「メンバーが始めた開発に、別の人が途中から合流して一緒にClaude Codeとやりとりする」が
技術的に成立するかを、**手元での実測**と調査で確認した記録。2026-07-25 実施 (Claude Code v2.1.219)。

## 結論 — 3つの別々の問いに分けて答える

混同すると設計を誤るため、分けて断定する。

| 問い | 答え |
|---|---|
| **① 複数人が同時に1セッションへ入って一緒に進める** | **不可能。回避策もない**(後述: 公式に「破壊的挙動」) |
| **② 他人のセッションを別マシンで後から引き継ぐ** | **可能。ただし非公式で脆い**(実測で確認) |
| **③ 指示・判断の共有と精査と引き継ぎ** | **完全に可能。公式サポートが厚い。ここが本命** |

→ **「合流」は同時共同編集ではなく、fork による分岐として設計する。**
(なお fork する以上「同じセッション」には決してならないため、**排他制御は不要**。proposal.md §8.2 参照)

---

## ① 同時参加は不可能 — これは前提として受け入れる

公式ドキュメントが明記している:

> fork せずに同じセッションを2つのターミナルで再開すると、両方のメッセージが1つのtranscriptに混ざる

ロック機構もマージ機構もない。transcript は append-only の JSONL なので、
2台から git 越しに同一セッションを進めれば**確実に破壊される**。これは機能不足ではなく事故。

公式機能も塞がれている:

| 機能 | 可否 | 理由 |
|---|---|---|
| Agent Teams | ✗ | teammate は全てAI。「Lead is fixed(リードの移譲不可)」「1セッション1チーム」と公式明記。**要求(1)の非階層性に構造的に反する** |
| `claude --teleport` | ✗ | 要件表に「Same account」と明記。他人のセッションは引き継げない |
| Remote Control | ✗ | 自分の別デバイスから自分のセッションを操作する設計 |
| claude.ai/code の Team可視性 | 閲覧のみ | 「開いた時点の最新状態。リアルタイム更新はしない」 |
| 公式のセッション共有 | 未実装 | issue #40981 は重複としてclose、#61640 は **closed as not planned**、#60082 は open のまま |

**①と②は矛盾しないので、読み違えないこと。**
①が言う「不可能」は**製品機能として提供されていない**という意味であり、
②が示す「可能」は**機構として動く**という意味である。特に #61640(他ユーザーによる共有セッションの再開)が
"not planned" である事実は、②の手法が動かないことを意味しない。実測では動く。
意味するのは **「Anthropic にはこの挙動を維持する義務がない」** ということであり、
これこそが②の形式リスクが重要な理由である。

なお #60082 のラベルが `area:cowork` である点は注目に値する。
**Anthropic 側に cowork 領域の開発線がある** → 自作部分は薄く保ち、独自フォーマットに深入りしないほうがよい。

---

## ② 引き継ぎは可能 — 実測で確認

### セッション履歴の保存形式

```
~/.claude/projects/<絶対パスの非英数字を「-」に置換した文字列>/<session-id>.jsonl
+ 同名のサイドカーディレクトリ <session-id>/ (subagents/, tool-results/, workflows/)
```

### 実測結果

セッションJSONLを、別の絶対パス(= 別マシン相当)に対応するディレクトリへコピーし、
そこから `claude --resume <id> --fork-session -p "..."` を実行:

| 検証 | 結果 |
|---|---|
| 移送先で会話履歴が読めるか | **✓ 読める**。最初のユーザー依頼を正しく回答 |
| 参照ファイルが存在しない環境でも読めるか | **✓ 読める**。`proposal.md` が無い場所でもレビュー内容の詳細を正確に再現 |
| ファイル内の `cwd` が旧マシンのままでも動くか | **✓ 動く**。再開後にClaude Codeが新しい値を追記して自己修復する |
| ファイル内の `sessionId` が不一致でも動くか | **✓ 動く** |
| 対応しない cwd から再開した場合 | ✗ `No conversation found with session ID:` で失敗 |
| `--fork-session` の挙動 | 新IDを発行し、**cwd を移送先の環境に書き換える**。元セッションは無傷 |

→ **必須条件は「起動時のcwd ↔ ディレクトリ名の対応」ただ1つ。**
デーモンが受け手の cwd に合わせてディレクトリ名を貼り替えるだけで合流が成立する。

Agent SDK ドキュメントにある「The cwd must match」は不正確で、実際にはパスを一致させる必要はなく
**貼り替えれば足りる**。

### 4つの留保

1. **JSONL の行フォーマットは公式が「内部仕様。どのリリースでも壊れうる」と警告している**
2. **サイドカーディレクトリの同梱が必要**。上記検証は本体JSONLのみ。subagent や永続化された
   tool-result を持つセッションでは `<session-id>/` 配下も運ばないと欠落する
   → ただし後述の「人が見る層」に絞る方式ではツール記録ごと落とすため、**この留保は解消する**
3. 別ユーザー(異なるホームディレクトリ)での再開は未検証。ただしパス不一致が許容されることは
   確認済みなので機構上は通ると見てよい
4. 文脈内に他人の絶対パスが残る。動作は止めないが誤誘導の元になる

### 先行実装が既にある: claude-git-sessions (ccgs)

- セッションを既存リポジトリの **orphan ブランチ `@ccgs/<name>`** に保存(main と履歴を共有せず、ソースを含まない)
- `ccgs push` / `ccgs pull` — pull 時に transcript の cwd を**自分のリポジトリルートへ書き換える**
- pull 後に `claude --resume <session-id>` で同僚のセッションを継続できる
- MIT。ただし **star 14 / commit 4 の早期段階**で、秘密情報のリダクション未実装・Git LFS非対応を自ら明記

→ **そのまま採用せず、参照実装として読む。**

### 公式の代替経路 SessionStore は、CLIユーザーには使えない ← 重要

`SessionStore` アダプタ(`append`/`load` 必須、`listSubkeys` でサイドカーも復元)は
「別ホストからの resume」を公式にサポートした唯一の機構で、バックエンドは S3/Redis に限定されず任意である。
一見すると「git を裏にした SessionStore を書けば公式APIの上で共有が成立する」ように見える。

**しかし確認した結果、これは CLI ユーザーには適用できない。**

| 問い | 答え | 根拠 |
|---|---|---|
| CLI からカスタム SessionStore を差し込めるか | **No** | settings.json / 環境変数 / CLIフラグのいずれにも設定手段が存在しない。Agent SDK の `query()` オプション専用 |
| `claude --resume` はローカル以外から読めるか | **No** | 公式ドキュメント: 「session ID lookup is scoped to the current project directory and its git worktrees」。`~/.claude/projects/` のみを探索する |

→ **SessionStore を使うには、チーム全員が対話TUIをやめて Agent SDK 駆動のツールに移行する必要がある。**
それは「ローカルのファイルを操作するような体験」(issue.md 基本方針)とは別物になる。

**したがって、合流は §2 の非公式な貼り替え方式に依存し続けることになる。**
これは受け入れるべきリスクであり、「将来より堅い公式経路へ移行できる」という逃げ道は**存在しない**。
先行実装の ccgs も同じ土俵に立っている。

緩和策は3つ:
1. **貼り替え層を薄く隔離する** — 壊れたらそこだけ直せる形にし、他の機能を巻き込まない
2. **指示ログ層は公式 hooks のみに依存させる** — 合流が壊れても要求(3)(4)(5)は生き残る
3. **Phase -1 で「そもそも重い合流が要るのか」を先に確かめる**(要らなければリスクごと消える)

---

## ③ 指示・判断の共有 — 公式サポートが厚い本命領域

### 指示の取得は公式インターフェースで完結する

`UserPromptSubmit` hook がユーザーの送信本文を渡す。実測ペイロード(v2.1.219):

```
session_id / prompt_id / transcript_path / cwd / permission_mode / hook_event_name / prompt
```

**注意: 公式ドキュメントの表には `user_message` と書かれているが、実機は `prompt`。**
バージョン間で揺れている可能性が高いので `prompt` と `user_message` の両対応にすること。

計画の捕捉は `PreToolUse` / `PermissionRequest` の matcher `ExitPlanMode` で可能。

### 引き継ぎの自動化ポイント

`SessionStart` hook の matcher に **`resume` と `fork`** がある。返り値で `additionalContext`(文脈注入)、
`sessionTitle`、`initialUserMessage` を指定できる。

→ **他人のセッションを resume した瞬間を検知して、指示ログ・未解決の判断・元の作者の意図の要約を
自動で読み込ませる**ことが公式APIだけで書ける。要求(5)の引き継ぎはこの1点に集約するのが最も筋が良い。

### 「同じ構成を全員に」は解決済みの問題

private git リポジトリに plugin marketplace を置き、プロジェクトの `.claude/settings.json` に
`extraKnownMarketplaces` + `enabledPlugins` を書くだけで、フォルダを trust した瞬間に
skills / hooks / subagent定義 / slash commands が全員へ配られる。**サーバー不要・階層なし・GA機能。**

→ 要求(1)「全員ほぼ同じ構成」はここで確定解が得られる。しかもこれが自作ツールの配布路そのものになる。

---

## 実データによる規模感

### セッションJSONLのサイズ (手元の3,050セッション)

| 指標 | 値 |
|---|---|
| 中央値 | 23 KB |
| 75パーセンタイル | 79 KB |
| 95パーセンタイル | 698 KB |
| 最大 | 63.4 MB |
| 合計 | 734 MB |

裾が重い。**全セッションのJSONLを常時git同期するのは非現実的。**

### 決定的な発見: 指示は極端に軽い

review.md を作成したセッション(111レコード・388KB)から実質的なユーザー指示を抽出した結果:

```
[1] 2026-07-24T20:07  (117字)
    issue.md を解決したいです、解決策として proposal.md が妥当か否かレビューしてください
```

**実質的な指示はわずか1件・117字。** 残りは全て AI 側の行動記録。

- **人間の意図は極小、AIの出力は膨大** — これがレビューが辛い理由そのもの
- **指示ログの共有はコストがほぼゼロ**。常時同期してよい
- **重いセッション履歴が要るのは「実際に合流する」瞬間だけ** → オンデマンドで足りる

---

## 何を共有すれば足りるか — 「人が見る層」だけで成立する

「全部同期は重い / 都度手動選択は辛い / 毎回要約させるのはトークンがきつい」という三すくみに対する解。
**共有対象を「人間が入力したテキスト」と「AIが人間に向けて話したテキスト(説明・提案・確認・計画)」に絞る。**
ツール呼び出しとツール出力は共有しない。

### 実測1: 分量(手元の大きめ12セッション、計231MB)

| 内訳 | 量 | 比率 |
|---|---|---|
| 人間の入力 | 844 KB | 0.4% |
| AIが人間に向けて話したテキスト | 819 KB | 0.4% |
| 計画・質問(ExitPlanMode / AskUserQuestion) | 46 KB | 0.02% |
| **共有する層 合計** | **1.7 MB** | **0.7%** |
| ツール入力 | 3.7 MB | 1.6% |
| **ツール出力(ファイル全文・秘密の入り口)** | **48 MB** | **20.7%** |

→ **共有層は中身ベースで1%未満。しかも最も危険なツール出力を構造的に含まない。**
要約が不要なのでトークン消費はゼロ、決定的に抽出できるので手動選択も不要。

### 実測2: 削った履歴から `--resume` できるか → **できた**

380KB のセッションから2つの変種を作り、別パスで再開を試した:

| 変種 | サイズ | resume | 評価 |
|---|---|---|---|
| **A: 人が見る層のみ**(tool_use / tool_result を除去) | 99 KB (26%) | **✓ 成功** | **採用**。文脈を保ったまま再開でき、ツール出力を一切含まない |
| B: 構造維持・ツール結果だけ伏字 | 343 KB (90%) | (未検証) | 削減効果が薄く採用理由がない |

※ 中身ベースで0.7%、ファイルベースで26%と差があるのは、残す各レコードに
uuid・timestamp・cwd 等の構造オーバーヘッドが付くため。

**Aで失われるもの・残るもの**:

- 残る = **筋書き**。何を頼み、AIが何を提案・説明し、どう合意したか
- 失われる = **証拠**。読んだファイルの中身、コマンドの出力

実際、Aで再開したセッションは議論の経緯を正確に想起した一方、
ファイル本文を引用する必要がある問いには「読み取り権限がないので確認できない」と答えた。
**合流先はリポジトリを持っているので、必要なら読み直せばよい。** 実用上の劣化は小さい。

→ **スナップショットは「全部か手動選択か」の二択ではない。決定的なフィルタで第三の選択肢が作れる。**

## commit ↔ プロンプトの紐付けは、どこに置くと壊れないか

「このコード変更はどのやりとりから生まれたか」を辿れるようにするための紐付けを、
どこに格納すべきかを実験で確かめた。

### 実測: squash merge は commit trailer を破壊する

feature ブランチの各コミットに `Cowork-Prompt: p-aaa111` という trailer を入れ、
`git merge --squash` した結果:

| 格納先 | squash merge 後 | 判定 |
|---|---|---|
| feature ブランチ各コミットの trailer | **完全に消える。** squashコミットのメッセージは新規に作られ、元のtrailerを引き継がない | **✗ 使えない** |
| squash コミット自身のメッセージの trailer | **残る**(`%(trailers)` で取得できた) | **✓** GitHubはPR本文をsquashメッセージに使うので、**PR本文にtrailerを書けば実現できる** |
| git notes | 特定のcommitハッシュに紐づくため、squashで生まれた新コミットには付かない。元コミットもブランチ削除+gcで到達不能になる | **✗ 使えない** |
| cowork-state 側に持つ | git の履歴操作の影響を受けない | **✓ 最も堅い** |

さらに rebase・cherry-pick でもハッシュは変わる。**コミットハッシュを主キーにした紐付けは脆い。**

### 実測: squash メッセージの設定は2種類あり、両方に対応できる

GitHub の squash コミットメッセージは**リポジトリ設定**で決まり、
「PR本文を使う」と「元コミットのメッセージを連結する」の2通りがある(排他)。
チームのリポジトリごとに設定が違いうるので、**どちらでも動く方式**が要る。

**ID行を「各コミットのメッセージ」と「PR本文」の両方に書く**方式を実測した:

| リポジトリ設定 | squash 後の結果 | grep で取得 |
|---|---|---|
| 元コミットのメッセージを連結 | ID行が**2回**出現(元コミットの数だけ) | **✓** |
| PR本文を使う | ID行が**1回**出現 | **✓** |

**両設定で成立した。** 重複は grep では無害。
rebase merge と merge commit は元コミットが履歴に残るので、コミット側のID行がそのまま生きる。
→ **4つのマージ方式すべてをこの1手でカバーできる。**

### 結論

- **主キーはコミットハッシュではなくスレッドID**(ハッシュは rebase/squash で変わる)
- **ID行は「各コミットのメッセージ」と「PR本文」の両方に書く**(どちらの設定でも1本は生き残る)
- **取り出しは `%(trailers)` による構造parseではなく、ID文字列の grep**。
  `%(trailers)` は最終段落しか見ないため、GitHub が付ける `Co-authored-by` が別段落に入ると壊れる
- あわせて **cowork-state 側にも紐付けを持つ**(git の履歴操作の影響を受けない冗長経路)
  ```json
  {"thread":"0042","repo":"your-repo","pr":123,"merged_commit":"7d1cbad","prompts":["p-aaa111"]}
  ```

git 側だけだと cowork-state を失うと辿れず、cowork-state 側だけだと git 単体で辿れない。
両方持てば**どちらからも辿れる**。

**コミット側へのID行の入れ方**: `commit-msg` フックは `core.hooksPath` の手動設定が要り
plugin marketplace で配れないため使わない。**CLAUDE.md の規約**(コミット時に必ずID行を入れる)とし、
コミットの大半を Claude Code 自身が作る前提に乗せる。抜けは coworkd が検出して一覧に表示する。

## 秘匿情報 — これは後付けが効かない

**transcript には tool_result としてファイルの全文・ls出力・絶対パスが平文で入る**
(調査では1つの tool_result だけで15KB超のソース全文が格納されていた)。

これを git に commit すると、`.env` を読んだセッションや `.gitignore` 済みファイルを読んだセッションの
中身が**リポジトリ履歴に永久に残る**。

そして**自動リダクションは構造的に困難**:

- `PostToolUse` はツール実行**後**に発火するため、その時点で生の値は既にJSONLに書かれている
- `@メンション`によるファイル展開は全フックを迂回する
- redaction hooks の作者自身が「意味のある保護として頼るな」と警告している

→ 防御は3段構え:
1. **共有時に gitleaks/trufflehog で検知したら push をブロック**
2. **そもそも秘密を読ませない権限設計**
3. **共有は本人が選んだセッションのみ**(全セッション自動収集は選択肢から外す)

---

## 意図ドリフト検知 — 実装可能な機構が3つある

「過去に意図していた内容が途中で失われていないか」(要求4)について。
**既製品は存在しない**が、出荷済み部品の連結で実運用レベルに届く。

### 発見1: 意図が失われる主因は「物忘れ」ではなく「compaction による物理的削除」

Governance Decay 論文の内訳が決定的:

| 状況 | 制約違反率 |
|---|---|
| 要約後も制約が残ったケース | **0%** |
| 要約で制約が落ちたケース | **38%** |

→ **対策の優先順位は「検知」より先に「そもそも落とさない配置」**(Constraint Pinning)。

- `CLAUDE.md` / `MEMORY.md` は compact 後も**再注入される**
- Skill の説明一覧は `noSurviveCompact: true` で**再注入されない**
- 会話要約は元の約12%に圧縮される

→ **絶対に守らせたい制約は Skill ではなく CLAUDE.md 側に置く。**

### 発見2: compaction 前後の差分が、そのままドリフト検知器になる

- `getSessionMessages()` は **post-compaction chain**(エージェントが実際に見ている履歴)を返す
  — 503個の生エントリがあるセッションでも18メッセージしか返らないことがある
- `store.load(key)` は **pre-compaction の生履歴**を返す

→ **この2つの差分が「当初の指示のうち、いま文脈から消えているもの」の一覧になる。**
さらに `UserPromptSubmit` のログは compaction で消えない追記専用の意図記録なので、これを正として照合できる。
`PreCompact` hook は圧縮を block できるので、重要な意図が落ちる直前に警告も出せる。

### 発見3: 決定的に検知したいなら LLM 判定ではなく REQ-ID の引用規律

traceSDD の実証結果:

| 方式 | 幻覚要求の検出率 | 偽陽性 |
|---|---|---|
| per-line の REQ-ID 引用 | **86〜88%** | **0%** |
| 引用なし | 0% | — |

spec に無いIDがコードにある = 捏造、spec にあるIDがコードに無い = 取りこぼし。
**grep と集合差分だけで判定できる**ためCIで機械的に落とせる。

→ **人が人を指摘する構図にならないので、要求(1)の非階層性とも整合する。**

### 発見4: `/goal` が既製品として存在する

`/goal`(v2.1.139+)は当初要求と「変えてはならない制約」を最大4000文字に畳み込み、
**毎ターン終了後に小型モデルが会話全体を照合**して未達判定+理由を返す。`--resume` でも条件が復元される。

実体は session-scoped な prompt-based Stop hook。
→ **より汎用の Stop hook を `settings.json` に書けば、同じ仕組みを「全セッション共通・チーム全員同一」に
一般化できる。settings.json をリポジトリにコミットすれば、MGRもメンバーも同じ逸脱チェックが自動で走る
= 承認者という役割を作らずに要求(1)を満たせる。**

注意: `/goal` の評価器はツールを実行せずファイルも読まず、**会話に表面化した内容だけを見る**。
条件文は「テストがパスすること」ではなく「`npm test` を実行し exit 0 を出力に示すこと」のように、
証拠が会話に落ちる書き方にする必要がある。

---

## 対称性 — 先行事例からの設計入力

### 「誰が何をどこまで確認したか」の共有は空白領域

per-file の確認フラグは Gerrit(AccountPatchReviewDb)にも GitHub(Viewed チェック)にもあるが、
**どちらもアカウント単位の私的な状態で、チームに共有されない**。共有 read receipt の先行事例は事実上ない。

**これは新規性であると同時に警告でもある**: 未読を可視化すると監視・詰問の装置になり、
要求(1)の「優劣をつけない」を内側から壊す。

Gerrit の attention set が「誰が見ていないか」ではなく **「今誰のターンか」だけ**を示し、
しかも "not super smart" と割り切って手動操作を前提にしているのは、この罠を避けるための設計判断と読める。
さらに attention set は**権限体系から完全に分離**されており、権限を持たない人がボールだけ回せる。

→ この知見は当初「バトン(ターンの受け渡し)」の根拠としたが、最終的には**バトン自体を置かない**判断に至った。
合流が必ず fork である以上、守るべき排他が存在しないため(proposal.md §8.2)。
残るのは「今誰のAIが動いているか」の**表示**だけで、これはロックではない。

### 確認は真偽値ではなく段階

Linux カーネルは語彙を分けている: `Acked-by`(目は通した) / `Reviewed-by`(技術的問題がないという意見) /
`Tested-by`(動かした) / `Signed-off-by`(受け渡し経路にいた)。

→ 「承認/未承認」ではなく `read` / `understood-intent` / `ran` / `object` のような対称的な動詞にすれば、
階層を作らずに確認の質を記録できる。**要求(4)の「意図が失われていないか」は
`understood-intent` の有無として表現できる。**

### GitHub の required approvals は既に対称

**階層を作るのは CODEOWNERS であって required approvals ではない。**
ruleset で「Require a pull request」+ required approvals を N に設定し、**CODEOWNERS を使わない**だけで、
「write権限を持つ誰でもいいから N 人」という完全対称のゲートになる。
「最後に push した人以外の承認を要求」オプションも併用できる。

→ **M-of-N の対称ゲートを自作する必要はない。**

### 可視化だけで、承認ゲートなしに効く

CHI 2026 の実験結果: **AIとのやり取りがピアに見えているだけで**(HHAI-shared条件)
AI生成コードへの依存が有意に下がり、適用前に理解する責任感が生まれた。

→ 当初設計していた「MGRが承認するレビューツール」が解こうとしていた問題の相当部分は、
**承認ゲートを置かずに指示の可視化だけで解ける可能性が高い。**

### 監視に転ばせない決定的な軸

**共有が「本人起点で選択的(author-initiated, curated)」か「自動で網羅的(auto-capture, complete)」か。**
前者なら知識共有、後者なら監視として受け取られる。

---

## 設計への含意(まとめ)

1. **合流は fork による分岐**。同時共同編集は不可能なので設計に入れない。
   **fork する限り同一セッションにはならないので、排他制御(ロック)は不要**
2. **共有は2層** — 指示ログ(軽量・常時) と セッション履歴(重量・本人起点でpublish)。
   **この2層は依存関係を切っておく**: 重い層は非公式機構に依存するが、軽い層は公式 hooks だけで成立する
3. **収集は公式インターフェース経由**(hooks)。JSONL 直接パースは最小限に閉じ込める
4. **意図ドリフトは「落とさない配置」を先に、「検知」を後に**。CLAUDE.md への固定 → Stop hook による毎ターン照合 → REQ-ID の集合差分
5. **秘匿情報の対策は後付けが効かない**。共有機構の必須の一部として最初から組み込む
6. **未読ではなくターンを可視化する**。「誰が見ていないか」を出すと監視装置になる
7. **自作範囲は薄く保つ** — plugin marketplace、GitHub required approvals、`/goal`、ccgs 等の既製部品を最大限使う
