# goal.md — 目的と展開計画

> 位置づけ: issue.md(課題)の上位にある「何のためにやるか」。
> 個別の解決策は solution.md、検証は phase-minus-1.md。
> 本書は、検証結果をどう読むか(判定の目的関数)と、投資の順序を決める基準を与える。

## 1. 目標の階層

| 層 | 内容 |
|---|---|
| 北極星 | AIとの協業で、超高品質なアウトプットを**低コスト・低認知負荷で**安定して出せる状態 |
| 第一課題 | 人間の介在 — 中断からの再開、途中の判断、レビュー — がコストと品質の律速になっている。典型が PR レビュー: 意図や観点といった押さえるべき勘所が共有されないまま、大量のコードを初見で判断させられる(issue.md) |
| 手段 | 人間の介在点をプロセスとして設計し、その安定運用を支える最小のツール群を作る |

原則: **プロセスが主、ツールは従。**
ツールは「介在点の運用が人力では安定しないと実証された箇所」にだけ作る。
批判的検証(phase-minus-1)の判定はスコープが2つある:
プロジェクト全体の導入可否は決めない(目的は精査であって門番ではない)。
一方で個別機構(Join 等)の作る/作らないは直接決める — それが「次にどの介在点へ
投資するか」の決定であり、「作らない」は撤退ではなく投資の後回しを意味する。

## 2. 人間の介在点マップ

AI協業の1タスクに人間の判断が乗る点を5つに分ける。
新しい要求・機構・FBは、すべてこのマップのどこかに割り付けてから優先度を議論する。

**既定は AI 駆動。** 介在点は「人間が作業する場所」ではなく「人間の判断が乗る場所」であり、
起草・提案・検出は AI に寄せ、人間の関与は確定・修正・拒否に限る
(AIが下書きを出し人が確定する — solution.md の昇格の原則と同じ)。

| # | 介在点 | 乗る判断 | 支える機構(現状) | 方向・未カバー |
|---|---|---|---|---|
| 1 | 意図の付与 | この作業で何を実現したいか | capture(最初のプロンプトを自動記録) | **意図は最初のプロンプトに現れる**(仮説)。人間が別途 intent を書く必然性は低く、AI がプロンプトから起草し人が確定する方向。プロンプトに現れにくい要素(完了条件・やらないこと)の扱いが未解決 |
| 2 | 途中の判断 | 質問への回答、方針転換 | capture(指示・回答・AI返答の自動記録) | 判断の要約の起草→確定の定型化 |
| 3 | 中断と再開 | 区切りの確定、現在地の承認 | brief。変更との紐づけはスレッドIDのコミット/PR本文記載(decision-records.md) — **実態(2026-08-02): ID 行の記載は実践ゼロで、紐づけは機能していない**。「支える機構」欄は設計上の想定であって現況ではない | **区切りを人間が意図的に制御するのは難しい。** AI が「まとめますか?」と提案する、または AI が判断する方向。指示単位での変更(commit/diff)との紐づけと表示 |
| 4 | レビューと承認 | 方針・判断・成果物の妥当性 | receipt 3段階(未検証)、brief のレビュアー利用 | 概要→詳細の段階的開示、確認の失効(未実装) |
| 5 | 習熟と展開 | プロセス自体の改善、展開の判断 | phase-minus-1 の記録シート | 軽量な評価(§3)、段階ごとの投資(§4) |

補: 同時実行時に衝突をどちらが引き取るかの判断(solution.md「C. 合流する」)は 2 の一種だが、
チーム形態でしか実測できないため §4 の S3 で検証する。

## 3. 期待どおりに働いているかの評価

**評価のための専用作業を作らない。** ベンチマーク整備にコストを割くのは本末転倒であり、
評価は運用の副産物と主観で足りる:

- **主観点数**: レビューする側・途中から合流する側が、その場で「やりやすさ」を5段階で1行記録する。大まかな傾向が分かれば十分
- **利用者のFB**: 気づいた不足はその場で挙げる。検証の記録に現れた不足だけを投資候補にする(思いつきでは足さない)
- **副産物の観察**: 通常のレビューがそのまま品質判定になる(完了条件を満たしたか)。同一タスクで「brief だけの判断」と「成果物を見た判断」が両方発生する場合は、結論が一致したかを1行残す — 一致するなら「brief で軽く済ませ、必要な所だけ掘る」が成立している証拠になる

## 4. 展開の段階(習熟モデル)

一気にチームへ広げず、段階ごとに検証対象を絞って進める。
**各段階に入る前に、その段階に必要な機能・体験を揃える**
(判定で「作る」となった機構を、いつ実装するかはここで決まる)。

| 段階 | 形態 | 検証すること | 入る前に揃えるもの |
|---|---|---|---|
| S0 | 1人 | 記録が自動で溜まるか、運用が軽いか | hook 登録のみ(済) |
| S1 | 1人2役(Run 1) | テンプレートの穴、運用摩擦、仮説の棄却(「引き継げた」の肯定側は確証とせず S2 で再検証) | 現状のツールのみ |
| S2 | 2人(Run 2) | 引き継ぎとレビューの確証(1人2役では取れない) | S1 判定で「作る」となった機構 + **視認性・体験性の底上げ**(他人に使ってもらえる水準の表示・導入の容易さ) |
| S3 | チーム(3人以上) | 一覧・バッジ・運用の定着、同時実行の引き取り判断 | 配布(plugin 等)・同期の仕組み |

## 5. コミットすること

細かい手続きのルールは設けない。コミットするのは2つだけ:

- 本機能が適切に動作し、検証と実用に耐えるものになるよう全力を尽くす
- 意図的に虚偽の報告や偽装をしない(実験の結果は、都合が悪くてもそのまま残す)
